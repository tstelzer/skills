[doc('Create a changeset')]
[group('Release')]
changeset:
    pnpm run changeset

[doc('Show changeset status')]
[group('Release')]
changeset-status:
    pnpm run changeset:status

[doc('Apply changesets to package versions')]
[group('Release')]
changeset-version:
    pnpm run changeset:version
