# EditorConfig

Copy [`reference/.editorconfig`](./reference/.editorconfig) to the repository root, then add language-specific
sections only when the repository needs them.

Use four spaces for source and Markdown. Use two spaces for YAML, JSON, JSONC, and Nix.
