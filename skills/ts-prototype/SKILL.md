---
name: ts-prototype
description: Prototype with minimal code. Only explicitly triggered by user.
---

## Rules

- Apply alongside any task skill the user invokes. 
- Follow `skill: ts-principles`.
- Write only the code needed to try the requested behavior.
- Avoid automated tests. Add only the minimal amount when you are certain later changes will keep the behavior they cover.
